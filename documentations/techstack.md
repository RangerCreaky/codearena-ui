Core UI
shadcn/ui — copy-paste components you own, Radix primitives, fully accessible

Styling
Tailwind CSS v4 — utility-first, CSS-native, no config file needed

Animation
motion/react (Framer Motion) — page transitions, exit animations, spring physics

Icons
Lucide React — shadcn's default, 1300+ icons, tree-shakeable

Data / Tables
TanStack Table v8 — headless, sorting/filtering/pagination, virtualization

Charts
shadcn charts (Recharts wrapper) — swap Nivo or Observable Plot for complex viz

Forms
React Hook Form + Zod — schema validation, no controlled-input rerenders

State / Server State
Zustand — local UI state, zero boilerplate
TanStack Query — server state, caching, optimistic updates

Auth
Clerk — drop-in UI, managed, zero effort
Auth.js (NextAuth) — open source alternative, full control

